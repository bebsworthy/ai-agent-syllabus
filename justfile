# Ai Agent Syllabus

# Start Starlight dev server
dev:
    cd starlight && npm run dev

# Install dependencies
install:
    cd starlight && npm install

# Build the site
build:
    cd starlight && npm run build

# Preview the built site
preview:
    cd starlight && npm run preview
